#!/usr/bin/perl -w
#tic tac toe game for hackday
use strict;
use warnings;

$count = 0
@board=(1..9);
print "Please select X or O for Player One: "
my $player_one = <STDIN>;
chomp $player_one;
for ($count = 1; $count < 10; $count++){
    if ($)
}

print @board;